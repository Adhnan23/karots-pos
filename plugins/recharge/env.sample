# Mobile Recharge plugin — environment
#
# The recharge plugin needs no extra environment variables: it reads the core
# database, settings (receipt printer/width, currency) and cash-drawer session
# through the Core API. This file exists so the bootstrapper can merge any future
# plugin settings into the shop's .env.sample alongside the core variables.
