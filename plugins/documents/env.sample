# Print & Copy plugin — environment
#
# The documents plugin needs no extra environment variables: it reads the core
# database, settings (currency, receipt printer/width) and the sale/expense/stock
# services through the Core API, and runs its own migrations under
# goose_db_version_documents. This file exists so the bootstrapper can merge any
# future plugin settings into the shop's .env.sample alongside the core variables.
