APP_ENV=development
DATABASE_URL=postgres://pos_user:pos_password@localhost:5432/pos_db?sslmode=disable
SERVER_PORT=3000
# Must be at least 32 characters.
JWT_SECRET=dev-secret-change-me-0123456789abcdef
# Access token TTL. For the cookie-based UI this doubles as the session length,
# so a shift-length value (e.g. 12h) is convenient. API clients refresh tokens.
JWT_EXPIRES_IN=12h
JWT_REFRESH_EXPIRES_IN=168h
CORS_ORIGINS=http://localhost:3000
# Fallback CUPS queue names (must be RAW queues) used when the matching dropdown
# in Admin → Settings → Printers & Labels is left on "System default". Leave both
# empty to use the system default printer. See PRINTING.md.
RECEIPT_PRINTER=
LABEL_PRINTER=
